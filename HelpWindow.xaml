<Window x:Name="window" x:Class="ElementPeriodTable.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElementPeridTable"
xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"        
        mc:Ignorable="d"
        Title="帮助" Height="450" Width="800" Icon="/icon.png" WindowStartupLocation="CenterOwner" Loaded="window_Loaded" SizeChanged="window_SizeChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Text="{Binding Source.AbsoluteUri, ElementName=WV2, Mode=OneWay}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="{Binding FontSize, ElementName=window}" IsReadOnly="True"/>
        <wv2:WebView2 x:Name="WV2" Source="https://shiningyangyxn.github.io/ElementPeriodTable/" Initialized="WV2_Initialized" Grid.Row="1"/>
    </Grid>

</Window>
